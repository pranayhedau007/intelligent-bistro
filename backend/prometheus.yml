global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "intelligent-bistro-backend"
    static_configs:
      - targets: ["host.docker.internal:3001"]
    metrics_path: /metrics
